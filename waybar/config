{
  "layer": "top",
  "position": "top",
  "height": 48,
  "margin": 10,

  "modules-left": ["hyprland/workspaces", "hyprland/window"],
  "modules-center": ["custom/spotify", "clock"],
  "modules-right": [
    "tray",
    "pulseaudio",
    "backlight",
    "network",
    "disk",
    "memory",
    "cpu",
    "battery", 
    "custom/battery-mode",
    "custom/keymap"
  ],

  "hyprland/workspaces": {
    "active-only": false,
    "persistent-workspaces": {
      "*": 3
    }
  },

  "hyprland/window": {
    "format": "{title}",
    "icon": true,
    "icon-size": 24,
    "max-length": 40,
  },

  "custom/spotify": {
    "exec": "python /home/jin/.config/waybar/script/spotify.py --player spotify",
    "format": "{}  ",
    "return-type": "json",
    "on-click": "playerctl play-pause",
    "on-scroll-up": "playerctl next",
    "on-scroll-down": "playerctl previous"
  },

  "clock": {
    "interval": 1,
    "locale": "zh_CN.UTF-8",
    "format": "{:L%H:%M:%S}"
  },
  

  "tray": {
    "spacing": 8,
    "icon-size": 24
  },

  "pulseaudio": {
    "format": "󱡒 {volume}%",
    "scroll-step": 5
  },

  "backlight": {
    "device": "intel_backlight",
    "format": " {percent}%",
    "on-scroll-up": "brightnessctl set 5%+",  
    "on-scroll-down": "brightnessctl set 5%- && if [ $(brightnessctl get) -lt 100 ]; then brightnessctl set 100; fi"
  },

  "network": {
    "interface": "wlan0",
    "interval": 1,
    "format": "  {essid} {bandwidthDownBytes}",
    "tooltip-format": "{ipaddr}",
    "format-disconnected": "无连接"
  },

  "disk": {
    "format": " {percentage_used}%",
    "path": "/"
  },

  "memory": {
    "interval": 1,
    "format": " {percentage}%",
    "tooltip-format": "内存使用：{used:0.1f}G / {total:0.1f}G"
  },

  "cpu": {
    "interval": 1,
    "format": " {usage}% {max_frequency}GHZ",
  },

  "battery": {
    "interval": 10,
    "states": {
      "warning": 30,
      "critical": 15
    },
    "format": " {capacity}%"
  },

  "custom/battery-mode": {
    "exec": "~/.config/waybar/script/battery-mode.sh",
    "signal": 2,
    "return-type": "json",
    "on-click": "~/.config/waybar/script/battery-mode.sh --toggle & pkill -RTMIN+2 waybar",
    "format": "{}"
  },

  "custom/keymap": {
    "exec": "/home/jin/.config/waybar/script/keymap.sh",
    "return-type": "json",
    "format": "{}"
  }
}
